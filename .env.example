# AI App Studio Environment Variables Template
# Copy this file to .env and update the values

# GitHub Token for PR creation (optional)
# GH_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx

# TMUX session configuration
TMUX_SESSION=cc
# TMUX_SESSION=ai-demo  # Alternative session name

# Claude Code command configuration
# Default command with minimal permissions
CLAUDE_CMD="claude code --dangerously-skip-permissions --allowedTools 'Bash Edit' --add-dir ."
# Alternative with more tools:
# CLAUDE_CMD="claude code --dangerously-skip-permissions --allowedTools 'Bash Edit Read Write' --add-dir ."

# Project root (auto-detected if in git repo)
# ROOT=/path/to/project

# Bus daemon polling interval (milliseconds)
# BUS_POLL_INTERVAL=500

# Log level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO

# Max concurrent child agents
# MAX_CHILDREN=10

# Child agent timeout (seconds)
# CHILD_TIMEOUT=3600

# Enable debug mode
# DEBUG=false